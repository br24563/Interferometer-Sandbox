# Interferometer Sandbox

A dependency-free, browser-based Michelson interferometer simulator.

Open `index.html` in a modern browser, then adjust the wavelength, arm lengths,
phase shift, and visibility to explore interference at the detector.
